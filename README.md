# ПОЗИТРОН КЕНО v6.3 CLEAN

Чистая сборка 6.3 с единым обучающим FINGERPRINT.

## Архитектура
- `app-v63.js` — интерфейс и единая загрузка тиражей.
- `engine-v63.js` — LOGIC / ANTILOGIC, Manhattan, переходы, баланс, M1–M20, исторические состояния и обучение весов.
- `storage-v63.js` — единая IndexedDB: резерв тиражей, прогнозы, факт, попадания, обученные веса.
- `styles-v63.css` — интерфейс.
- `sw.js` — network-first service worker без отката на старый кэш.

Архив FINGERPRINT открываемый: прогноз → факт → попадания → веса после обучения.
